import { connect } from 'react-redux';
import { } from '../actions';
import ListGuideInvited from '../component/ListGuideInvited'

const mapStateToProps = state => {

    return {

    };
};

const ListGuideInvitedContainer = connect(
    mapStateToProps,
    {

    }
)(ListGuideInvited);

export default ListGuideInvitedContainer;
