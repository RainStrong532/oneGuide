import { connect } from 'react-redux';
import { } from '../actions';
import ListGuideTour from '../component/ListGuideTour'

const mapStateToProps = state => {

    return {

    };
};

const ListGuideTourContainer = connect(
    mapStateToProps,
    {

    }
)(ListGuideTour);

export default ListGuideTourContainer;
