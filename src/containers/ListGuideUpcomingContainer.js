import { connect } from 'react-redux';
import { } from '../actions';
import ListGuideUpcoming from '../component/ListGuideUpcoming'

const mapStateToProps = state => {

    return {

    };
};

const ListGuideUpcomingContainer = connect(
    mapStateToProps,
    {

    }
)(ListGuideUpcoming);

export default ListGuideUpcomingContainer;
